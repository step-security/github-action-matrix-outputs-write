name: 'Matrix outputs - write (filtered)'
description: 'Write outputs for matrix. Patched to filter artifact download.'
author: codingsloth@pm.me
branding:
  icon: 'chevrons-up'
  color: 'white'
inputs:
  matrix-step-name:
    required: false
    description: "Matrix step name"
  matrix-key:
    required: false
    description: "Matrix key"
  outputs:
    required: false
    description: "YAML structured map of outputs"
outputs:
  result:
    description: "Outputs result (Deprecated!!!)"
runs:
  using: "node20"
  main: "index.js"
